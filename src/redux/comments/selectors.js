/** @format */

export const commentsState = state => state.comments.items;

export const statusLoadingStateComments = state => state.comments.isLoading;

export const statusErrorComments = state => state.comments.error;
